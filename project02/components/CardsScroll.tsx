import { StyleSheet, Text, View, ScrollView } from "react-native";
import React from "react";

const CardsScroll = () => {
  return (
    <View>
      <ScrollView>
        <Text>CardsScroll</Text>
      </ScrollView>
    </View>
  );
};

export default CardsScroll;

const styles = StyleSheet.create({});
